<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OAUID" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="statusBarBackground">@color/white</item>
        <item name="statusBarForeground">@color/colorBlack</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Theme.OAUID" parent="Base.Theme.OAUID" />
</resources>